name: React Native CI/CD

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Set up Java JDK
        uses: actions/setup-java@v2
        with:
          distribution: 'adopt'
          java-version: '17'

      - name: Install dependencies
        run: |
          npm install

      - name: Run tests
        run: |
          npm test

      - name: Prepare for iOS Build
        run: |
          echo "Preparing for iOS Build..."
          echo "Installing CocoaPods..."
          sudo gem install cocoapods
          cd ios
          echo "platform :ios, '12.0'" > Podfile
          echo "use_modular_headers!" >> Podfile
          echo "target 'CICD' do" >> Podfile
          echo "  pod 'React', :path => '../node_modules/react-native'" >> Podfile
          echo "  pod 'React-Core', :path => '../node_modules/react-native/React/Core'" >> Podfile
          echo "  pod 'React-Core/DevSupport', :path => '../node_modules/react-native/React/Core/DevSupport'" >> Podfile
          echo "  pod 'React-RCTActionSheet', :path => '../node_modules/react-native/Libraries/ActionSheetIOS'" >> Podfile
          echo "  pod 'React-RCTAnimation', :path => '../node_modules/react-native/Libraries/NativeAnimation'" >> Podfile
          echo "  pod 'React-RCTBlob', :path => '../node_modules/react-native/Libraries/Blob'" >> Podfile
          echo "  pod 'React-RCTImage', :path => '../node_modules/react-native/Libraries/Image'" >> Podfile
          echo "  pod 'React-RCTLinkingIOS', :path => '../node_modules/react-native/Libraries/LinkingIOS'" >> Podfile
          echo "  pod 'React-RCTNetwork', :path => '../node_modules/react-native/Libraries/Network'" >> Podfile
          echo "  pod 'React-RCTSettings', :path => '../node_modules/react-native/Libraries/Settings'" >> Podfile
          echo "  pod 'React-RCTText', :path => '../node_modules/react-native/Libraries/Text'" >> Podfile
          echo "  pod 'React-RCTVibration', :path => '../node_modules/react-native/Libraries/Vibration'" >> Podfile
          echo "  pod 'React-RCTWebSocket', :path => '../node_modules/react-native/Libraries/WebSocket'" >> Podfile
          echo "end" >> Podfile
          # Add other pods as needed
          pod install

      - name: Create IPA
        run: |
          echo "Creating IPA..."
          echo "Build the project using Xcode or run a script"
          echo "For example, you can create a simple script like 'build_ipa.sh' in the ios folder"
          echo "Contents of 'build_ipa.sh':"
          echo "#!/bin/bash"
          echo "xcodebuild -workspace CICD.xcworkspace -scheme CICD -configuration Debug -destination 'generic/platform=iOS' archive -archivePath build/cicd.xcarchive CODE_SIGN_STYLE=Automatic CODE_SIGNING_REQUIRED=NO CODE_SIGN_IDENTITY= PROVISIONING_PROFILE="
          echo "xcodebuild -exportArchive -archivePath build/cicd.xcarchive -exportOptionsPlist exportOptions.plist -exportPath build/cicd"
          echo "mv build/cicd/*.ipa ../debug-ios.ipa"
          echo "chmod +x build_ipa.sh"
          echo "./build_ipa.sh"

      - name: Upload Debug IPA artifact
        uses: actions/upload-artifact@v2
        with:
          name: debug-ios
          path: debug-ios.ipa
